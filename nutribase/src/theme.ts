"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#764248",
        },
        secondary: {
            main: "#CACEB7",
        },
    },
});

export default theme;
